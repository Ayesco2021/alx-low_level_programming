#include<stdio.h>

/**
 * _ispuuer: upper function
* @c: is a character
* Return: 1 (c is upper)
* Error: 0 (otherwise)
*/

int _isupper(int c)
{
	if (c <= Z)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
